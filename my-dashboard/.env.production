# Production API origin.
# If you deploy the Cloudflare Worker, override this in the deployment provider
# with the Worker URL described in CLOUDFLARE_HYPERDRIVE.md.
VITE_API_URL=https://dashboard.ritzmediaworld.in
